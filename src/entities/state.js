export default () => {
  const width = null;
  const height = null;
  const gameStart = null;
  const startTime = null;
  const endTime = null;

  return {
    width,
    height,
    gameStart,
    startTime,
    endTime,
  };
};
