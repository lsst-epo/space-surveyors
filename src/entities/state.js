export default () => {
  const boundingRect = null;
  const gameStart = null;
  const startTime = null;
  const endTime = null;

  return {
    boundingRect,
    gameStart,
    startTime,
    endTime,
  };
};
