export default (boundingRect, aspectRatio) => {
  const gameStart = null;
  const startTime = null;
  const endTime = null;

  return {
    aspectRatio,
    boundingRect,
    gameStart,
    startTime,
    endTime,
  };
};
