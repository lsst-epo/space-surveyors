import { GameEngine } from 'react-game-engine';
import styled from 'styled-components';

const StyledGameStage = styled(GameEngine)`
  background-color: steelblue;
  width: 100%;
  height: 100%;
`;
export default StyledGameStage;
